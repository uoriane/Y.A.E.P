# Supabase Edge Functions

## Future Implementation

Outbound email delivery through Edge Functions is planned for a future phase.

Current submission scope uses in-app notifications stored in `public.notifications` and displayed on dashboards.

When domain/provider setup becomes available, email sending can be activated in a later release.
